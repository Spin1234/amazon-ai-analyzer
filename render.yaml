# services:
#   - type: web
#     name: amazon-ai-analyzer
#     env: python
#     buildCommand: "pip install -r requirements.txt"
#     startCommand: "gunicorn app:app"


services:
  - type: web
    name: amazon-ai-analyzer
    env: python
    buildCommand: |
      pip install -r requirements.txt
      playwright install chromium
    startCommand: gunicorn app:app
